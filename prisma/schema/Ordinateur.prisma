model Ordinateur {
    id        Int     @id @default(autoincrement())
    adressMac String
    lastNameO       String
    dirstName    String
    mail      String  @unique
    passwordO  String
    age       Int?
    gender     String?
    userId    Int
    user      User    @relation(fields: [userId], references: [id])
}
