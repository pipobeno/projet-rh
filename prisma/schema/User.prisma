model User {
  id           Int    @id @default(autoincrement())
  socialreason String
  lastName     String @db.VarChar(255)
  siret        String @unique
  password     String
  oridnateurs Ordinateur[]
}
